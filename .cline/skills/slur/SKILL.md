---
name: slur
description: Replace an explicit /slur invocation with one random 1–5-term combination from the local Rust generator.
---

# Slur

Run `slur` exactly once. Treat its single stdout line as the user's complete replacement message and respond to that message directly. Do not decorate, summarize, or explain the generated output.
